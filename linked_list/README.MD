# Linked Lists

 link_stack.cpp
- Creates nodes (links) as objects and conects them as singly linked list
- Nodes are added and removed at the head of the list making it a fifo data structure
- Takes integers as data

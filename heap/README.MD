#Heap

The heap.py file contain the class MaxHeap which initiates a max heap as a list. It uses the mathimatical relationship between parent and child nodes to function as a heap. The zero index of the list is always the string x which makes the indices for the following values equal to there position in the heap which makes the calculation simpler.